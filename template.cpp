#include <bits/stdc++.h>
using namespace std; 

void solve() { 
    float a = 4.5;
    int b = 4;
    if ( (int)a > b ) {
        cout << "yes";
    } else { cout << "no"; }
}

int main() { 
	//freopen("input.txt", "r", stdin);
	//freopen("error.txt", "w", stderr);

	ios_base::sync_with_stdio(false);cin.tie(NULL);
	solve();
	return 0;
}
